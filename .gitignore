.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env
.DS_Store

# Claude Code local settings, memory, and session data.
.claude/

# Raw pulled data — regenerated by src/fetch_usgs.py, so no need to commit it.
# The final data lives in the SQLite database, not here.
data/raw/

# Cleaned data — regenerated by src/clean_data.py, so no need to commit it either.
data/processed/

# MLflow artifact store — model files logged during training. Large and
# regenerable; the run metadata (params/metrics) lives in mlflow.db instead.
mlruns/