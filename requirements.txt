# Project dependencies — Earthquake Risk Forecasting
# Install with:  pip install -r requirements.txt

# requests: makes HTTP calls to the USGS earthquake web service (pulls the data)
requests==2.32.3

# pandas: tables (DataFrames) for organizing, analyzing, and cleaning the data
# NOTE: pinned to 2.3.3 (was 3.0.3) — MLflow requires pandas < 3, so installing
# MLflow downgraded it. 2.3.x is the stable line; our scripts run fine on it.
pandas==2.3.3

# matplotlib: draws graphs so we can visually analyze the earthquake data
matplotlib==3.11.0

# seaborn: sits on top of matplotlib for nicer statistical graphs with less code
seaborn==0.13.2

# plotly: interactive graphs (hover, zoom, pan) — used for the geographic quake
# map and the live presentation; seaborn/matplotlib handle the static report graphs
plotly==6.8.0

# shapely: geometry tools — computes each quake's distance to the nearest tectonic
# plate boundary (the distance_to_fault_km feature added in clean_data.py)
shapely==2.1.2

# scipy: statistical tests — used for correlation p-values in the EDA notebook
scipy==1.18.0

# jupyterlab: notebook environment for exploring and analyzing the data
jupyterlab==4.6.1

# scikit-learn: the logistic-regression baseline + train/test tooling (Week 3).
# Installed automatically as an MLflow dependency.
scikit-learn==1.9.0

# mlflow: experiment tracker — logs each model run (features, settings, PR-AUC/F1)
# so we can compare the 4 ablation variants reliably in one dashboard (Week 3).
mlflow==3.14.0

# NOTE: sqlite3 (our database) is built into Python, so it needs no install here.
# Later weeks will add: xgboost (Week 3-4), apache-airflow (Week 4), fastapi (Week 5).
